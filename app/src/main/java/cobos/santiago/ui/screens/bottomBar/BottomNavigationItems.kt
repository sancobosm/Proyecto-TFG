package cobos.santiago.ui.screens.bottomBar

/*AnimatedNavigationBar(
                modifier = Modifier.height(50.dp),
                selectedIndex = selectedIndex,
                cornerRadius = shapeCornerRadius(cornerRadius = 20.dp),
                ballAnimation = Parabolic(tween(300)),
                indentAnimation = Height(tween(300)),
                barColor = MaterialTheme.colorScheme.primary,
                ballColor = MaterialTheme.colorScheme.primary
            ) {
                navigationBarItems.forEach { item ->
                    Box(
                        modifier = Modifier
                            .fillMaxSize()
                            .noRippleClickable {
                                selectedIndex = item.ordinal
                                *//*when (item) {
                                    NavigationBarItems.Person -> navHostController.navigate(AppScreens.Profile.route)
                                    NavigationBarItems.Home -> navHostController.navigate(AppScreens.HomeScreen.route)
                                    NavigationBarItems.Settings -> navHostController.navigate(AppScreens.Settings.route)
                                }*//*
                            }, contentAlignment = Alignment.Center
                    ) {
                        Icon(
                            modifier = Modifier.size(30.dp),
                            imageVector = item.icon,
                            contentDescription = "Bottom Bar icon",
                            tint = if (selectedIndex == item.ordinal) MaterialTheme.colorScheme.onBackground
                            else MaterialTheme.colorScheme.inversePrimary
                        )
                    }
                }
            }*/